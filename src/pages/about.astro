---
import BaseLayout from "../layouts/BaseLayout.astro";
import { SITE } from "../config";
import { t } from "../i18n";

const lang = "zh";
const socials = Object.entries(SITE.socials).filter(([_, url]) => url);
---

<BaseLayout title={t(lang, "about.title")} lang={lang}>
  <section class="about">
    <img src={SITE.author.avatar} alt={SITE.author.name[lang]} class="avatar" onerror="this.style.display='none'" />
    <h1>{SITE.author.name[lang]}</h1>

    <div class="prose about-content">
      <p>😎德州小高的奇妙人生——一位码农的自白</p>
      <p>大家好！我是德州小高，一个上海制造，加拿大升级，美国运行的全球游牧码农。🎭</p>
      <h2>🚀 人生版本更新日志</h2>
      <ul>
        <li>v1.0（1977）：上海发布，童年 bug 颇多，但自带探索精神。</li>
        <li>v2.0（2006）：移民加拿大，顺利获得"枫叶护盾"，切换新服务器。🍁</li>
        <li>v3.0（现在）：流浪到美国，正在自定义人生脚本，尝试成为一名 Solopreneur，目标是用代码打造被动收入帝国！💰💡</li>
      </ul>
      <h2>🎮 职业生涯——从码农到创业者再到自由人</h2>
      <ul>
        <li>曾开过游戏公司，梦想打造下一个《马里奥》，结果踩了太多坑，硬是把人生变成了"魂系游戏"👾。</li>
        <li>大厂打工人经验丰富，见过资本主义魔法，但最终还是决定逃离 9-5，成为自己的老板。</li>
        <li>现在准备 All in Solopreneur，目标是"自己赚钱，自己花，不看老板脸色"！😎</li>
      </ul>
      <h2>🏆 人生信条</h2>
      <ol>
        <li>代码要跑，钱包要鼓，人要自由！</li>
        <li>能用自动化解决的问题，绝不手动操作！（包括赚钱）</li>
        <li>人生就像创业，不试一次，怎么知道自己会不会爆单？</li>
      </ol>
      <p>🚀 <strong>新旅程刚刚开始，欢迎大家围观，一起见证一个 Solopreneur 的崛起！</strong>💡💰</p>
    </div>

    {socials.length > 0 && (
      <div class="social-links">
        {socials.map(([name, url]) => (
          <a href={url} target="_blank" rel="noopener noreferrer" class="social-link">{name}</a>
        ))}
      </div>
    )}
  </section>
</BaseLayout>

<style>
  .about {
    text-align: center;
    padding: 3rem 0;
  }

  .avatar {
    width: 150px;
    height: 150px;
    border-radius: 50%;
    object-fit: cover;
    margin-bottom: 1.5rem;
    border: 3px solid var(--primary);
  }

  h1 {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .about-content {
    text-align: left;
    max-width: 600px;
    margin: 0 auto 2rem;
  }

  .social-links {
    display: flex;
    justify-content: center;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .social-link {
    padding: 0.5rem 1.25rem;
    border: 1px solid #e2e8f0;
    border-radius: 8px;
    text-transform: capitalize;
    transition: all 0.2s;
  }

  .social-link:hover {
    border-color: var(--primary);
    background: var(--primary);
    color: white;
  }
</style>
